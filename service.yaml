apiVersion: v1
kind: Service
metadata:
  name: playground
spec:
  selector:
    app: playground           # MUST equal the Deployment's pod labels, or 0 endpoints
  ports:
    - port: 8888
      targetPort: 8888        # wil42/playground listens on 8888
